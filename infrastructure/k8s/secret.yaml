apiVersion: v1
kind: Secret
metadata:
  name: gamevallies-secret
  namespace: gamevallies
type: Opaque
stringData:
  # Database credentials (MySQL + Redis)
  # IMPORTANT: Replace with actual values before deploying
  DATABASE_URL: "mysql://gamevallies:CHANGE_ME_PASSWORD@mysql:3306/gamevallies"
  MYSQL_ROOT_PASSWORD: "CHANGE_ME_PASSWORD"

  REDIS_URL: "redis://:CHANGE_ME_PASSWORD@redis:6379/0"
  REDIS_PASSWORD: "CHANGE_ME_PASSWORD"

  # JWT secrets (generate with: openssl rand -base64 32)
  JWT_SECRET: "CHANGE_ME_JWT_SECRET_32_BYTES_BASE64_ENCODED"
  JWT_REFRESH_SECRET: "CHANGE_ME_JWT_REFRESH_SECRET_32_BYTES_BASE64_ENCODED"

  # External API keys
  LLM_API_KEY: "CHANGE_ME_DEEPSEEK_API_KEY"

  # Email configuration
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  SMTP_USER: "noreply@gamevallies.com"
  SMTP_PASSWORD: "CHANGE_ME_SMTP_PASSWORD"

  # Basic auth for internal services
  INTERNAL_API_KEY: "CHANGE_ME_INTERNAL_API_KEY"

  # Admin credentials (use for initial setup only)
  ADMIN_EMAIL: "admin@gamevallies.com"
  ADMIN_PASSWORD: "CHANGE_ME_ADMIN_PASSWORD"
