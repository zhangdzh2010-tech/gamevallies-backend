# 2026-09-09 生产种子作品试运行与修复记录

## 更换模型后的复测（15:43 UTC）

用户更换模型后，在已发布的后端`19165260ae6e1565677f6f89182cb2963ea2278f`上新提交2条作品；独立日志位于`tmp/showcase-20260909/cohort-model-switch/`。本次未修改模型配置或生产代码。

| 作品 | 任务耗时 | 结果 |
| --- | ---: | --- |
| 一盏茶时 | 174.0秒 | 意图解析完成；代码生成两次调用分别60184ms、60100ms，均返回上游504，无HTML |
| 潮汐拾贝 | 60.2秒 | spec_build意图解析阶段返回上游504，无HTML |

计时器调用事件明确记录模型`deepseek-v4-pro`、provider `ZLtokens`，配置版本`1788968497`，无备用provider。配对游戏在路由快照持久化前失败，未获得模型字段，不能把计时器的模型信息冒用为配对游戏的已证实配置。两者错误地址均为`https://www.zltokens.com/v1/chat/completions`。

- 计时器作品`d6deac6c-afc0-4953-a246-ec6b0b83276d`，任务`150cc193-8a5c-471a-82f0-91721aa19b71`，15:43:13.388至15:46:07.386 UTC。
- 配对游戏作品`78729996-7d43-4801-8174-ad60500bc2e2`，任务`6ee1015a-f270-4f7b-a3c5-c6088ddad823`，15:43:17.539至15:44:17.749 UTC。

本次观察生成成功率0/2，不能评价新模型的成品质量，也未证实99%目标。未发布新作品。失败额度已自动返还，账户本期30次、已用11次，剩余19次。

计时器只记录两次底层代码生成请求，未发生多轮候选重生成；但它走interactive_creation分支，配对游戏又未到达代码生成，因此本批不能作为casual_arcade质量循环重试修复的线上覆盖证明（该修复已有本地/CI回归）。约60秒收到HTTP 504是上游响应，不是本任务1800秒总预算耗尽；仅凭应用侧证据不能确定是网关自身还是其下游模型服务的超时。暂停扩大批次，需先恢复该网关实际生成请求的可用性或由用户配置有效备用线路。

## 恢复执行后的状态

最新补充修复`19165260ae6e1565677f6f89182cb2963ea2278f`已生产发布成功，运行`34368211656`所有验证和release通过。已停止本轮新生成，等待模型路由恢复。20条生产展示作品和99%成功率仍未完成或证实。

用户已授权提交与生产部署。`40b2735`首次CI因旧集成测试仍依赖空锚点插入而被拦截，未发布；测试夹具修正后，`1d9eaea33b67cd2d8680fd5a17863cb5ef45e06e`发布成功，GitHub运行`34364385362`全部作业通过，AI/game/content版本为47/46/43。

电脑崩溃后核对了发布产物与日志，未重复提交中断前的操作。新版本复测2条新作品并迭代光学作品1次，全部因模型网关`https://www.zltokens.com/v1/chat/completions`返回504失败，尚未到达作品验收：

- 计时器：作品`344a7f91-9bb1-4e2c-8438-4aadba83f055`，任务`a56106c4-ec9f-45ef-a9fd-e384074c5f00`，136.9秒，两次上游调用失败。
- 配对游戏：作品`d5439f96-3e90-41c4-8a48-db4b803e69cb`，任务`3a86d44e-efa5-4d0f-a65a-13d1abec9694`，398.9秒，六次上游调用失败。
- 光学迭代：任务`4b2a08c1-4375-4ac4-b33d-e8df1282dc11`，上游504失败，未修复角弧。

这批路由快照显示provider名称为`kimi-k3`，无备用provider；此前成功的光学初次生成使用`deepseek-v4-pro`。本次没有修改模型路由或凭据。当前创作额度仍为19，失败创建已自动退回。等待用户确认模型配置或恢复可用上游，不继续重复提交同一故障。

另修复了上游重试放大：底层LLM客户端已用尽重试/备用路由后，HTTP或网络异常不能再作为代码质量错误触发三轮完整生成。通过异常因果链识别运输故障，保留路由快照与原始异常，并让质量循环立即结束；不依赖错误文本，不把真实语义错误误分为上游错误。相关116项回归通过，覆盖400/401/429/502/503/504、超时、异常链环、原有模型切换和质量修复。此保护不能修复上游服务，也不能代替备用路由。

## 第一阶段结论（部署前）

目标为20条可展示作品。本轮已规划20条，实际提交3条新作品，另对其中1条做了2次迭代；**尚无作品通过全部人工验收，未发布任何新作品，99%目标未达到或证实**。未删除失败记录、修改额度或充值。后续批量暂停，等待本地修复获准提交和部署。

生产基线：backend `9629c29e5723b45b2d84417bcb051a243fc6dd04`，frontend `3db4165`。后端部署SHA已由成功发布任务的release产物核对；本文件所述本地修复尚未部署，不能归入修复后的线上成绩。

## 真实任务结果

| 作品 | 初次生产生成耗时 | 生产生成结果 | 人工验收 |
| --- | ---: | --- | --- |
| 一盏茶时 | 155.6秒 | 失败，两轮候选均被动画推进检查拒绝 | 未通过 |
| 潮汐拾贝 | 597.0秒 | 失败；包含完整重生成、质量补丁及纠正，最终出现inputBound未定义 | 未通过 |
| 光的折返 | 96.7秒 | 生成完成 | 第一版布局不合格；第二版空画布；第三版恢复绘图但角弧仍不正确 |

光学作品两次迭代分别耗时68.5秒、70.4秒，不计为新增独立样本。第三版预览一次加载出现`Failed to fetch`，手动重试后成功。最终正式展示验收为0/3；仅按“生成任务返回作品”的窄口径为1/3，不能拿后者代替质量合格率。

任务索引：

- 一盏茶时：作品`322e5f55-325a-4f79-bb32-12efeee12134`，任务`6db109a8-3433-401e-8901-93d5a3150e7a`。
- 潮汐拾贝：作品`4b607409-e90c-463b-a68b-462eebfe3ab0`，任务`6bfa9f3c-a7ea-425b-8567-96d030781993`。
- 光的折返：作品`e432cfab-cabf-4804-a27e-36cb0418ef28`；初次任务`1d4244c8-bcac-4b5c-bcde-4beba1bd8687`，迭代任务`d2449dc8-7de3-4e3e-9560-21c767d3b3e1`、`ef63343b-d8d8-420b-8649-53679c52cdf7`。

## 已实施的本地修复

1. **正常秒级倒计时被误杀。** 旧检查仅观察750ms，合法的每秒更新无法稳定通过。改为100ms分段观察，最长2200ms，发现变化立即结束。仍拒绝仅改变按钮文字但不推进的计时器。以原HEAD重放新增回归用例，确认失败；新实现通过。生产失败计时器未保存候选HTML，因此该局部复现证明检查缺陷，不冒称已逐行检查原始作品。
2. **空代码分区诱导作用域破坏。** 标记归一化会在IIFE前插入空CONFIG/INPUT等分区；旧协议将其宣称为安全补丁锚点。实际失败作品的外部handler引用IIFE内的`inputBound`。现在仅列出非空锚点，拒绝向空脚本分区插入代码；整段替换同样受保护，提示明确保留闭包作用域。该保护不能替代全部JavaScript语义检查。
3. **失败交互作品无法回放。** 最终失败保留候选HTML及交互/分类审核报告，通过现有作者权限控制的任务产物接口保存，不把源码塞入进度消息。
4. **大桌面验收与真实预览尺寸不一致。** 增加1000×460、1000×600检查，保留1366×768、1920×1080。当前自动门槛仍主要检查横向溢出和内容变化，不声称已自动证明首屏构图、科学正确性或全部控件行为。
5. **科学绘图和初始化约束不足。** 生成提示加入尺寸观察、延迟初始化及重绘要求；审核提示加入箭头端点、坐标映射、角弧方向检查。提示加强不能保证模型执行正确，仍需逐作品的数值和视觉验收。

## 验证

Python3.12隔离环境及Playwright Chromium执行以下回归，**80 passed，1项既有Pydantic弃用警告**：

```sh
python -m pytest tests/test_interactive_creation.py tests/test_artifact_quality.py tests/test_quality_gate_patch_repair.py tests/test_section_patch.py -q --tb=short
```

工作目录为`packages/ai-engine`。另验证日志脱敏能移除密码/预览凭据而保留inputTokens/outputTokens计数，`git diff --check`通过。没有将这些模拟或本地测试计入生产生成成功率。

## 复测与交付条件

- 先审阅、提交和部署上述代码，核实实际部署SHA，等待旧任务结束后再开新批次。
- 先复测计时器、卡片配对和光学三个失败场景；通过后继续剩余17个创意。重试已失败创意也须保留原任务分母。
- 补上沙箱中由隐藏到显示的Canvas浏览器回归，并验证画面而非仅数值标签；第三版光学角弧尚待修复。
- 核对全部内部生成和补丁次数。该记忆游戏外层`retryCount=0`与事件中的多轮候选不一致，不能直接据此声称一次成功；此统计字段问题尚未修复。
- 20/20也只能说明该批次观察值。参照本目录外的`tests/acceptance/README.md`，至少299个具有代表性的独立样本全部成功，才支持单侧95%精确二项下界超过99%；额度与样本设计需另行确认。

20条选题及逐项验收要求在`tests/acceptance/showcase-20260909.json`。显式提交、记录证据与通过验收后发布的工具在`scripts/production_showcase.py`，不会自动重试、充值或发布。脱敏实测证据和完整事件日志位于本地忽略目录`tmp/showcase-20260909/`；每次迭代按任务ID单独留档，避免覆盖原始失败证据。登录凭据未写入文件。
