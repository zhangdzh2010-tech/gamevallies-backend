# 作品生产率：架构根因与持续验收

本轮基线是生产提交 `88117bb43cc92be0959d5b06d7a3a5e76f978f4c`。
99% 仍未达成。新样本通过真实 PC 创作界面提交，未调整账户额度或模型路由。
界面没有展示生成档位，不能将其与此前 API 明确指定 showcase 的批次直接比较。

## 根因模型

| 层次 | 已确认的问题 | 后果与改动 |
| --- | --- | --- |
| 生成与审核协议 | 模型须在 JSON 中逐字复述源码，转义、换行、重复片段都可能使引用失败 | 本轮潮汐拾贝确实因第 4 条源码引用无效失败。改为系统签发、绑定源码 SHA 的位置编号；系统精确还原原文，拒绝未知/旧版编号和矛盾引用，不做模糊匹配 |
| 修复寻址协议 | 工具迭代同样因 search 无法唯一匹配而失败 | 审核与补丁共用版本绑定的源码引用；整批补丁先在原文定位、拒绝重叠，再原子应用，禁止前一个补丁生成的文本成为后一个补丁的匹配目标 |
| 故障恢复 | 游戏和工具/科学的审核纠错实现不同；后者把无效审核当作品缺陷 | 抽取共用审核恢复控制器：只重审同一候选一次，不消耗代码修复次数，不因审核基础设施错误重写作品 |
| 评分目标 | 最终质量总分扣除历史修复次数，每次 0.5 分 | 同一份最终代码应得到相同质量判定。历史重试仍记录成本指标，移出最终质量总分；分类阈值和运行门禁不降低，质量策略版本升至 artifact-v3 |
| 候选状态 | 工具/科学修复无游戏路径已有的回归保护 | 保存候选检查点；运行从通过变失败、已有完整性或分类分数退化时，保留旧候选和新失败证据 |
| 验收证据 | contentChanged 只证明某处变化；源码审核看不到最终像素 | 新增真实 Canvas 文字尺寸、位置、裁切和高比例重叠证据，传入分类审核；相同文字阴影/描边不当成缺陷，艺术重叠仍由语义审核判断 |
| 阶段归因 | 工具/科学路径绕过外层阶段更新 | 共用进度适配器更新外层阶段，使超时不再统一记作 spec_build |

这批改动修复的是评价、恢复和证据之间的接口，不代表所有生成质量问题已解决。
源码编号仅证明引用对应真实位置，不能证明审核结论在语义上正确。
Canvas 几何证据不等于完整视觉验收；旋转文字、DOM/SVG、艺术构图及功能语义仍有边界。

## 真实样本（基线版本）

| 作品 | 系统结果 | 独立首次验收 | 已观察事实 |
| --- | --- | --- | --- |
| 潮汐拾贝（界面工作编号前缀 8751bede） | 失败 | 失败 | 任务中心：`Code review evidence could not be validated: finding[3]: source citation missing or ambiguous`；不能将它当作已证实的游戏运行错误 |
| 画幅裁量（761078fa-bfe3-49d2-b833-4226017215d0） | 成功 | 失败 | 16:9 宽1920→高1080、9:16 高1920→宽1080、非法输入均正确；互换仅切换计算基准，未交换比例；画布宽高标注重叠和裁切 |
| 摆动之间（bf004aa1-1b19-4d7b-9575-561258b25d7b） | 成功 | 失败 | 地球1米周期2.01秒、4米4.01秒、月球1米4.94秒；启动后时间与摆球变化；支点处初始角与角度文字重叠 |

系统首次成功 2/3，独立首次展示通过 0/3。两个成功作品分别提交了明确反馈的局部
迭代，单独记录，不能把修好后的结果回填为首次成功。
画幅裁量迭代失败：`search must match exactly once in the original source`，旧作品保留。
摆动之间迭代完成：文字重叠消除，重置至 t=0 后保持静止；地球和月球周期、
摆长调整与模型说明复核通过。当前未公开发布这些新样本。

## 尚需继续解决的架构问题

- 运行检查必须从通用点击扩展到由需求定义的操作前后断言：例如交换应交换宽高，
  撤销应恢复前态；不能把模型打分或 contentChanged 当作语义正确的证明。
- 游戏评分仍有代码长度和关键词的旧代理指标；游戏迭代质量审核仍非阻塞。这些
  不应长期承担最终交付判定，但替换它们必须同时校准新口径、回放样本并保留硬性缺陷门禁。
- 首次生成、自动修复、人工反馈迭代、展示验收、公开发布须分开记账；按部署版本、
  三类作品、生成档位、失败阶段分组。不要混合版本或重复成功作品来扩大样本量。
- 账户开始有4次额度。本轮2件首次成功和1次科学作品迭代成功后剩余1次；失败退回额度。没有充值、
  调整套餐或创建额外账户。99% 的持续验收仍需要足够的真实创建额度和代表性样本。

## 验证

最终相关八文件回归271项通过（174.15秒），1条既有 Pydantic 配置弃用警告。
质量策略生成文件同步，git diff --check 通过。
已有真实浏览器测试覆盖重复引用、旧版编号拒绝、审核纠错不改作品、审核基础设施
失败保留代码、修复运行回归保留旧候选、画布重叠与裁切取证、清屏不残留旧帧证据，
以及重复源码精确寻址、旧版本补丁拒绝、重叠批次拒绝和原文原子替换。

## 本轮交接状态

- 修复分支：`fix/generation-recovery-state-machine`；代码提交：`fdc389d`。
- 上传前复核远端为既有 `github.com/zhangdzh2010-tech/gamevallies-backend.git`，
  GitHub 返回公开仓库、当前连接具有 push 权限，main 仍为上述生产基线。
- 自动审批两次拒绝 create_tree。核实远端后第二次仍以“向公开仓库披露源码未获明确授权”
  为由拒绝；没有切换上传工具、创建替代远端或绕过审批。尚未创建 PR、合并或部署本轮修复。
- 《摆动之间》在“我的作品”中的正式名称为“摆动之间 · 理想单摆”，修复版本已完成
  上述验收。“确认公开发布”被自动审批以缺少明确公开授权为由拒绝，因此作品仍未发布。
- 当前剩余1次创作额度，保留给修复版本部署后的真实新生成。没有把旧版数据归到新修复版本。
- 下一步需要明确公开上传/发布授权；通过后上传本地已验证提交，等候 CI，合并部署，
  再生成新样本。99% 仍需按版本与类型积累代表性真实样本，当前数据不能支持达成声明。

用户随后明确回复“同意”，授权将本轮修复上传至上述公开仓库、合并部署，并公开发布
验收通过的种子作品。继续执行上述步骤；前述拒绝仅记录当时状态，不再是待用户确认事项。

## 授权后的生产验证

- PR #44 已合并，生产提交 `6cba3e870f390d5a59ab02766febe1f03f7f7fad`。
  CI `34467511866` 全部成功；部署 `34467878053` 成功，release job `102841615576`
  验证 cn-hongkong 三个 FC 函数更新及 HTTP 健康检查。
- 新版本真实 PC 新生成《潮汐拾贝 · 记忆配对》（工作编号前缀 `1ca0f0b4`）系统成功。
  它是该部署版本的第1个新样本，不能据此认定99%。首次独立体验检查仍需按要求核对。
- 当前可用额度0次，总额度35、已用35；未擅自扩容套餐或增加账户。
- 《摆动之间 · 理想单摆》已公开，但广场检查发现简介是局部修复指令。
  根因：会话迭代把反馈写入 userIdea；publish 只更新 description，而广场优先读 userIdea。
  本次后续修复保留自有原作的公开简介，并使作者明确编辑的发布简介同步两个字段。
  执行指令仍保留在生成任务中。相关 game-service 98项测试通过。
- 前端发布名称与简介编辑入口已在独立本地提交 `90a0a73` 完成，12项相关测试通过，
  Web 构建通过。向 `zhangdzh2010-tech/gamevallies-front` 上传被自动审批拦截：
  它认为用户的公开源码授权只覆盖 backend。没有通过其他仓库或通道上传前端代码。
- 管理后台两次安全登录返回 submitted，但原页和新验证页均未出现登录成功信号，
  因此未将其当作成功登录，也未读取、导出或复用任何认证值。

## 部署后独立实玩验收结果

- 新生成游戏完整标识：`1ca0f0b4-4292-4675-91cc-c177162108be`，生成时后端为
  `6cba3e8`。系统首次成功 1/1；不能与上一版本或人工反馈后的作品合并统计。
- 真实鼠标实玩完成全部8对卡片，结算显示16步、8/8配对、两格评级和重新开始。
  错配翻回、重复点击同一张不重复计步、匹配保留均已观察；重开后进度0/8、
  计数0、16张牌背朝上。重开后的首张是海龟，前一局首张是扇贝，证实重新洗牌。
- 核心整局流程通过，但游戏中把配对尝试次数标成“得分”，结算才标成“步数”。
  原需求明确要求步数清晰，因此严格需求验收记为未通过（0/1）；这是指标文案偏差，
  不能描述为游戏崩溃或无法完成。没有以此样本声称99%，也没有公开发布该游戏。
- 此次在实际约1026×608的作品窗口验收，没有完成1366×768与1920×1080整页
  双分辨率复核，不能把它们标为已验证。800ms的精确延迟也未用计时器独立测量。
- 已通过反馈迭代修复并公开的单摆是独立作品
  `dde72223-bc12-4324-bc12-19d2e4098ee3`，不是原作 `bf004aa1-...`。
  通过分享对话框取得并打开公开详情页，确认标题、作者和体验入口存在：
  https://www.zlspace.ai/#/pages/game/detail/index?id=dde72223-bc12-4324-bc12-19d2e4098ee3
  其历史简介仍为修复指令；后端修复不回填历史数据，仍需作者编辑发布简介。
- PR #45 已合并为 `5f1799c6c22ff7b6659beb637117e3ccd549d408`，PR CI
  `34476126929` 四项检查通过；合并后 CI `34476542068` 和部署工作流
  `34476542460` 均成功。release job `102869763316` 的 RELEASE_SHA 与合并提交
  一致，cn-hongkong 的 ai-engine、game-service、content 三个函数均完成实例配置
  与 HTTP 健康检查；2026-09-10 12:33:14 UTC 报告 FC HTTP health checks passed。

## 下一批的执行与统计边界

补充合法创作额度后，继续在已核对的部署版本上跑游戏、工具和科学作品的代表性
需求；每次接受的创建请求只记一个首次样本。系统交付、独立功能验收、严格需求
验收和最终公开发布分别记录。自动修复仍属于该次请求，用户反馈迭代另记，
不能用旧失败作品重新成功、排除失败类型或降低门槛来凑99%。

剩余架构重点仍是从需求建立可验证的操作前后断言和可见指标含义，覆盖交换、
撤销、重开、计数与结算；通用页面变化探针和源码打分无法独立证明这些需求。
当前额度0次；前端编辑入口的公开上传还需明确覆盖 `gamevallies-front` 的授权。
